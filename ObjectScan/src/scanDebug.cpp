#include "ObjectScan.h"

void Scan::ScanDebug()
{
    cout << "tokenPerLine: " << tokenPerLine << endl;
    /*
     cout << "tokenIndex: " << tokenIndex << endl;
     if (readyToMove) {
     cout << "readyToMove: cierto\n";
     } else {
     cout << "readyToMove: falso\n";
     } // end if //
     cout << "currentToken: " << currentToken << endl;
     */
} // end of Scan::ScanDebug() //
