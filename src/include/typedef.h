#ifndef TYPEDEF
#define TYPEDEF
  using real = double;
  //using real = float;

  using intPos = unsigned int;
  //using intPos = size_t;
 
  constexpr intPos zero = 0;
  constexpr intPos one  = 1;

#endif
